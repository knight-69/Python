# don't use s = {} as it will create a empty dicitionary.
# e = set() # it is called a empty set.

# the sets are used because the it does not repeat the value as the value will be only repeat as once only in output .
s = {1,5,32,5,5,5,"harry"}

print(s)

s.add(565) # we can add value in it .

print(s,type(s))