s1 = {1,45,6}
s2 = {7,8,1,78}
print(s1.union(s2)) # in this both the value gets added in one set without reapeating the common number b/w them .

print(s1.intersection(s2)) # in this the common value is showns as output.