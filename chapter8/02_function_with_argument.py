def greet(name, ending): # we can add add argument in this as many we want
    print("Good day," + name)
    print(ending)
    return "done" # so now we want the to have value other that none so we will use return function.

a = greet("harry","Thank you") # we can put the output of argument here what we want as the output at the terminal
greet("rohan", "Thank you")
greet("divya", "Thanks")